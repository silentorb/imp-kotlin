package silentorb.imp.parsing.syntax

enum class BurgType {
  argument,
  argumentName,
  argumentValue,
  bad,
  definition,
  definitionName,
  dot,
  expression,
  fileRoot,
  group,
  importClause,
  importPathToken,
  importPathWildcard,
  literalFloat,
  literalInteger,
  parameter,
  reference,
}
