package silentorb.imp.parsing.syntax

enum class BurgType {
  argument,
  argumentName,
  argumentValue,
  bad,
  definitionName,
  dot,
  expression,
  fileRoot,
  importKeyword,
  importPathToken,
  importPathWildcard,
  letKeyword,
  literalFloat,
  literalInteger,
  parameter,
  reference,
}
