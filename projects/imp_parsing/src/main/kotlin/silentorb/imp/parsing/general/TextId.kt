package silentorb.imp.parsing.general

enum class TextId {

  // Error messages
  duplicateSymbol,
  expectedIdentifier,
  expectedExpression,
  expectedNewline,
  importNotFound,
  invalidToken,
  multipleGraphOutputs,
  noGraphOutput,
  unexpectedCharacter,
  unknownFunction,
}
